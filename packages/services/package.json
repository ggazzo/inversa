{
    "name": "@brewpilot/services",
    "version": "0.0.0",
    "private": true,
    "main": "./src/index.ts",
    "types": "./src/index.ts",
    "description": "BleAdapter (abstract) + ConnectionManager. Platform impls live next to the apps. Populated in Phase B.",
    "peerDependencies": {
        "@preact/signals-react": "*"
    }
}
