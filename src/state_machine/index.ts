/**
 * Finite State Machine - TypeScript Implementation
 *
 * This library provides a flexible, type-safe implementation of a finite state machine
 * that can be used to model complex state transitions in applications.
 */

// Export the core FSM implementation
export {
  FiniteStateMachine,
  type StateConfig,
  type MachineConfig,
} from "./FiniteStateMachine";

// Export example functions if needed in a real-world application
export { simulateTrafficLight } from "./TrafficLightExample";
