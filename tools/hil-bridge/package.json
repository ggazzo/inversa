{
  "name": "inversa-hil-bridge",
  "version": "0.1.0",
  "description": "Host bridge for the Inversa hardware-in-the-loop test harness. Drives sensor inputs over USB CDC and analyzes telemetry against safety/PID invariants.",
  "private": true,
  "type": "module",
  "main": "dist/cli.js",
  "bin": {
    "hil-bridge": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "start": "tsx src/cli.ts"
  },
  "dependencies": {
    "serialport": "^12.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0"
  }
}
